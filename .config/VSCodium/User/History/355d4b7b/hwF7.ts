const config = {
  // CORE CONFIGURATION
  OBJECT_SIZE: 64,
  ROWS: 15,
  COLUMNS: 15,

  // PLAYER CONFIGURATION
  PLAYER_SPEED: 5,
  PLAYER_HEALTH: 1,
  PLAYER_BOMB_COUNT: 1,
  PLAYER_BOMB_RANGE: 1,
  PLAYER_CAN_PASS_PLAYERS: true,

  // BOMB CONFIGURATION
  BOMB_TIMER: 3000,

  // POWERUP CONFIGURATION
  POWERUP_SPAWN_CHANCE: 0.2,
};

export default config;
